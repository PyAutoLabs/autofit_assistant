# scripts/

Committed, runnable Python produced during sessions — every script follows the
workspace style in `skills/_style.md` "Generated script style" (notebook-convertible
narrative docstrings, `<Project>:<path>` citations).

Throwaway plots and data dumps go to `scripts/scratch/` (gitignored). Persistent fit
results go to `output/` (gitignored).
